{
  "name": "bunori-runtime",
  "version": "1.0.0",
  "description": "Modular QuickJS browser runtime for Bunori extensions",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "build": "node build.mjs",
    "check": "npm run typecheck && npm test && npm run build",
    "sync:bunori": "npm run check && cp dist/runtime.js ../Bunori/app/src/main/assets/ln_reader_runtime.js && echo 'Successfully synced runtime to Bunori assets!'"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@noble/ciphers": "^2.1.1",
    "cheerio": "^1.0.0",
    "dayjs": "^1.11.13"
  }
}
