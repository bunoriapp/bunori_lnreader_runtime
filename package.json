{
  "name": "bunori-runtime",
  "version": "1.0.0",
  "description": "browser runtime for lnreader extensions in bunori",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "pretest": "npm run build",
    "test": "node --test",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@noble/ciphers": "^2.1.1",
    "cheerio": "^1.0.0",
    "dayjs": "^1.11.13"
  }
}
